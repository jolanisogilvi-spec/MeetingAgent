# MeetingAgent 重新开发提示词

你是一名资深全栈工程师。请重新开发一个名为 **MeetingAgent** 的会议智能体系统。

系统后端框架使用 **FastAPI**。除 FastAPI 外，不要对技术栈做过多强制约束；你可以根据项目规模和实现效率自行选择前端框架、数据库、ORM、任务队列、UI 组件库、模型 SDK、文件存储方案等。但最终系统必须结构清晰、可运行、可维护、可扩展。

请注意：这个系统不是一个简单的“上传文件生成会议纪要”工具，而是一个围绕会议生命周期构建的会议智能体应用。它需要管理会议、人员、部门、会议材料、AI 纪要、结构化结果、待办任务、模型配置和导出结果。

## 1. 产品目标

开发一个面向企业或团队内部使用的会议智能体系统，帮助用户完成：

- 创建和管理会议
- 管理人员与部门
- 选择会议参会人员
- 上传会议材料
- 粘贴会议文本
- 识别会议音频
- 生成会议摘要
- 生成结构化会议纪要
- 自动提取待办任务
- 集中管理会议任务
- 编辑结构化纪要 JSON
- 导出 Word 会议纪要
- 配置和测试大模型、Embedding 模型、语音模型

系统主流程：

```text
配置模型 -> 维护部门和人员 -> 创建会议 -> 上传或输入会议内容
-> 生成会议纪要 -> 查看摘要和结构化结果 -> 自动生成待办任务
-> 编辑确认 -> 导出 Word -> 在任务页跟进待办
```

## 2. 整体架构要求

后端：

- 使用 FastAPI。
- 提供 REST API。
- 提供自动 API 文档。
- 能处理 JSON 请求、表单请求、文件上传。
- 能托管前端静态文件，或与单独前端项目对接。
- 所有核心数据需要持久化保存。
- 模型调用错误、文件解析错误、参数错误必须有清晰返回。

前端：

- 需要有完整可用的 Web 页面。
- 页面不能把所有功能堆在一个页面。
- 页面左侧需要有清晰导航。
- 每个耗时操作必须有明确反馈。
- 模型测试和保存结果必须显示在对应配置卡片内。

技术栈选择原则：

- 除 FastAPI 外，其他技术自行选择。
- 不要为了炫技引入不必要复杂度。
- 第一版优先保证功能完整、结构清晰、便于二次开发。

## 3. 页面信息架构

左侧导航必须包含以下入口，并保持这个顺序：

```text
会议
日程任务
人员与部门
设置
```

### 页面风格要求

整体视觉应偏向企业级工作台：

- 深色紧凑左侧导航
- 明亮主工作区
- 清晰页面标题区
- 卡片式信息分组
- 表单、按钮、反馈状态清晰
- 不做营销页
- 不使用大面积装饰性内容
- 重点服务高频操作和信息扫描

## 4. 会议模块

会议是系统的核心对象。

### 4.1 会议列表

会议页面需要展示会议列表。

会议列表字段：

- 会议名称
- 会议时间
- 所属部门
- 参会人员
- 会议状态
- 最近更新时间

会议状态至少包含：

```text
draft       草稿
generating 生成中
completed  已生成
confirmed  已确认
failed     生成失败
```

会议列表需要支持：

- 查看全部会议
- 点击会议进入会议详情
- 刷新会议列表

建议支持：

- 按状态筛选
- 按部门筛选
- 按关键词搜索
- 按时间排序

### 4.2 新建会议

用户点击“新建会议”后，打开弹窗、抽屉或独立页面。

新建会议字段：

- 会议名称，必填
- 会议时间，可选
- 所属部门，可选
- 参会人员，可多选
- 会议类型，可选
- 会议目标，可选
- 备注，可选

创建成功后：

- 生成会议 ID
- 会议状态为 `draft`
- 保存创建时间和更新时间
- 自动进入该会议详情

### 4.3 会议详情

会议详情页面需要包含以下区域。

#### 基础信息

展示：

- 会议名称
- 会议时间
- 所属部门
- 会议类型
- 会议目标
- 当前状态

#### 参会人员

展示：

- 已选择参会人员
- 人员姓名
- 所属部门
- 职位或角色

#### 会议材料

支持两种输入方式：

1. 粘贴会议文本
2. 上传会议文件

会议文件支持：

```text
.mp3
.wav
.txt
.docx
```

知识库参考资料支持：

```text
.txt
.docx
```

知识库参考资料可以多选上传。

#### 生成会议纪要

会议详情中必须有“生成会议纪要”按钮。

点击后执行：

1. 校验当前会议是否存在。
2. 校验是否配置大模型。
3. 获取会议文本或解析上传文件。
4. 如果上传的是音频文件，调用语音识别模型转写。
5. 如果上传了知识库参考资料，解析资料内容。
6. 使用 Embedding 模型对知识库资料建立检索上下文。
7. 调用大模型生成会议摘要。
8. 调用大模型抽取结构化纪要 JSON。
9. 调用大模型对结构化纪要进行检查和补全。
10. 保存原文、摘要、结构化纪要到当前会议。
11. 从结构化纪要中的待办事项自动生成任务。
12. 更新会议状态。

生成过程中必须有加载状态和错误反馈。

## 5. 纪要结果模块

会议详情中需要包含结果区域，使用标签页或分区展示。

必须包含：

```text
摘要
结构化纪要
待办任务
JSON 编辑
原文
```

### 5.1 摘要

展示 AI 生成的会议摘要。

摘要要求：

- 中文
- 简明
- 覆盖核心议题、结论和待办
- 默认长度建议 200 到 400 字

### 5.2 结构化纪要

展示结构化 JSON 中的内容。

至少展示：

- 会议主题
- 关键讨论点
- 决策结论
- 待办事项

### 5.3 待办任务

展示从结构化纪要中提取出的任务。

任务字段：

- 责任人
- 任务内容
- 截止时间
- 状态

### 5.4 JSON 编辑

展示当前会议的结构化纪要 JSON。

要求：

- 用户可以手动修改 JSON。
- 保存时校验 JSON 格式。
- 保存成功后刷新结构化纪要和待办展示。
- 保存失败时显示明确错误。

### 5.5 原文

展示会议原始输入文本或音频转写文本。

## 6. 日程任务模块

日程任务页面用于集中管理所有会议中沉淀出的待办任务。

页面必须包含：

- 全部任务数量
- 待跟进任务数量
- 未明确责任人任务数量
- 任务列表
- 任务筛选

筛选项至少包含：

```text
全部任务
未明确责任人
未明确时间
```

任务字段：

- 任务 ID
- 所属会议
- 所属部门
- 责任人
- 任务标题
- 截止时间
- 任务状态
- 创建时间
- 更新时间

任务状态建议：

```text
todo
doing
done
delayed
```

第一版可以先实现任务展示。后续应支持：

- 编辑责任人
- 编辑截止时间
- 修改任务状态
- 删除任务
- 按人员筛选
- 按部门筛选
- 按会议筛选

## 7. 人员与部门模块

人员与部门用于支撑会议创建和任务归属。

### 7.1 部门管理

需要支持：

- 新增部门
- 查看部门列表
- 编辑部门
- 删除部门

部门字段：

```json
{
  "id": "dept_xxx",
  "name": "产品部",
  "description": "",
  "created_at": "",
  "updated_at": ""
}
```

### 7.2 人员管理

需要支持：

- 新增人员
- 查看人员列表
- 编辑人员
- 删除人员
- 绑定所属部门

人员字段：

```json
{
  "id": "person_xxx",
  "name": "张三",
  "department_id": "dept_xxx",
  "role": "产品经理",
  "email": "",
  "phone": "",
  "created_at": "",
  "updated_at": ""
}
```

新建会议时，必须能选择部门和参会人员。

## 8. 设置模块

设置页面必须分为三张配置卡：

```text
大模型
Embedding
语音模型
```

每张卡片都必须有：

- 保存按钮
- 测试连接按钮
- 当前卡片内的反馈区域

反馈区域需要显示：

- 保存中
- 保存成功
- 保存失败
- 测试中
- 测试成功
- 测试失败
- 失败原因

### 8.1 大模型配置

字段：

- API Key
- Base URL
- 模型 ID
- Temperature
- Max Tokens

保存要求：

- 只保存大模型相关字段。

测试要求：

- 只使用大模型配置。
- 发送一个简短测试 prompt。
- 返回模型是否可用。

### 8.2 Embedding 配置

字段：

- API Key
- Base URL
- 模型 ID

保存要求：

- 只保存 Embedding 相关字段。
- 不允许默认复用大模型 API Key。
- 不允许默认复用大模型 Base URL。

测试要求：

- 只使用 Embedding 配置。
- 调用一次 embedding 查询。
- 成功时返回向量维度。
- 失败时返回错误原因。

Base URL 处理要求：

- 用户可能填写 OpenAI-compatible 根地址。
- 用户也可能填写包含 `/embeddings` 的完整地址。
- 后端需要规范化，避免最终请求路径变成 `/embeddings/embeddings`。

### 8.3 语音模型配置

字段：

- 模式：本地 / 线上
- 语音模型类型
- API Key
- Base URL
- 模型名称

交互规则：

- 选择“本地”时，不显示 API Key 和 Base URL。
- 选择“本地”时，保存时清空 API Key 和 Base URL。
- 选择“线上”时，显示 API Key 和 Base URL。

测试要求：

- 本地模式：测试本地语音识别模型是否可加载。
- 线上模式：测试线上语音模型是否可连通。

## 9. 数据模型

### 9.1 Settings

```json
{
  "llm_api_key": "",
  "llm_base_url": "",
  "llm_model_name": "",
  "embedding_api_key": "",
  "embedding_base_url": "",
  "embedding_model_name": "",
  "speech_provider": "local",
  "speech_model_type": "whisper",
  "speech_api_key": "",
  "speech_base_url": "",
  "speech_model_name": "",
  "temperature": 0.3,
  "max_tokens": 1024
}
```

### 9.2 Meeting

```json
{
  "id": "meeting_xxx",
  "title": "项目周会",
  "meeting_time": "2026-05-20T10:00",
  "department_id": "dept_xxx",
  "participant_ids": ["person_xxx"],
  "meeting_type": "周会",
  "objective": "同步项目进度",
  "notes": "",
  "status": "draft",
  "raw_text": "",
  "summary": "",
  "meeting_json": {},
  "created_at": "",
  "updated_at": ""
}
```

### 9.3 Meeting JSON

大模型最终需要输出以下结构：

```json
{
  "Topic": "会议主题",
  "KeyPoints": ["关键讨论点"],
  "Decisions": ["决策结论"],
  "ActionItems": [
    {
      "Who": "责任人",
      "What": "任务内容",
      "When": "截止时间"
    }
  ]
}
```

要求：

- 字段不可缺失。
- 没有内容时使用空字符串或空数组。
- 不要返回额外解释文本。

### 9.4 Task

```json
{
  "id": "task_xxx",
  "meeting_id": "meeting_xxx",
  "department_id": "dept_xxx",
  "owner_name": "张三",
  "title": "整理项目排期",
  "due_date": "下周五",
  "status": "todo",
  "created_at": "",
  "updated_at": ""
}
```

## 10. API 要求

### 10.1 健康检查

```http
GET /api/health
```

### 10.2 设置

```http
GET /api/settings
PUT /api/settings/llm
PUT /api/settings/embedding
PUT /api/settings/speech
POST /api/settings/test/llm
POST /api/settings/test/embedding
POST /api/settings/test/speech
```

### 10.3 部门

```http
GET /api/departments
POST /api/departments
GET /api/departments/{department_id}
PUT /api/departments/{department_id}
DELETE /api/departments/{department_id}
```

### 10.4 人员

```http
GET /api/people
POST /api/people
GET /api/people/{person_id}
PUT /api/people/{person_id}
DELETE /api/people/{person_id}
```

### 10.5 会议

```http
GET /api/meetings
POST /api/meetings
GET /api/meetings/{meeting_id}
PUT /api/meetings/{meeting_id}
DELETE /api/meetings/{meeting_id}
POST /api/meetings/{meeting_id}/generate
POST /api/meetings/{meeting_id}/export
```

生成会议纪要接口需要支持：

- settings
- meeting_text
- meeting_file
- kb_files

### 10.6 任务

```http
GET /api/tasks
POST /api/tasks
PUT /api/tasks/{task_id}
DELETE /api/tasks/{task_id}
```

如果第一版时间有限，任务可以先实现自动生成和列表查看。

## 11. AI 工作流要求

会议纪要生成至少包含三个步骤。

### 11.1 摘要生成

输入：

- 会议全文
- 知识库上下文

输出：

- 中文会议摘要

### 11.2 结构化抽取

输入：

- 会议全文
- 知识库上下文

输出：

- Topic
- KeyPoints
- Decisions
- ActionItems

### 11.3 质检补全

输入：

- 会议全文
- 摘要
- 已抽取 JSON
- 知识库上下文

输出：

- 修订后的完整 JSON

要求：

- 检查遗漏的关键讨论点。
- 检查遗漏的决策。
- 检查遗漏的待办任务。
- 保持相同 JSON Schema。

## 12. RAG 要求

知识库参考资料处理流程：

1. 解析上传的 txt/docx。
2. 将文本切分为片段。
3. 使用 Embedding 生成向量。
4. 建立向量索引。
5. 使用会议全文作为 query 检索相关片段。
6. 将相关片段拼接进大模型提示词。

第一版可以使用临时向量库。

后续可以扩展为长期知识库：

- 保存资料文件
- 保存向量索引
- 支持按会议、部门、项目隔离资料
- 回答中展示引用来源

## 13. 语音识别要求

音频文件支持：

```text
.mp3
.wav
```

语音识别路线：

- 本地模型
- 线上模型

本地模型：

- 允许使用 Whisper 或其他本地语音识别模型。
- 测试时验证模型是否可加载。

线上模型：

- 使用用户配置的 API Key、Base URL、模型名称。
- 需要支持真实音频转写。
- 测试时验证接口是否可连通。

音频转写结果需要保存为会议原文。

## 14. Word 导出要求

会议详情需要支持导出 Word。

Word 内容至少包括：

```text
MeetingAgent 会议纪要

一、会议摘要
二、会议议题
三、关键讨论点
四、决策结论
五、待办事项
```

待办事项必须用表格展示：

| 责任人 | 任务内容 | 截止时间 |
|---|---|---|

导出文件名优先使用会议名称。

## 15. 前端交互要求

所有操作必须有反馈：

- 创建会议
- 新增部门
- 新增人员
- 生成会议纪要
- 保存 JSON
- 导出 Word
- 保存模型配置
- 测试模型连接

反馈要求：

- 不能只依赖浏览器控制台。
- 模型测试结果必须显示在对应模型卡片中。
- 失败信息要展示给用户。
- 长任务要禁用按钮或显示加载状态，避免重复提交。

## 16. 安全要求

必须注意：

- API Key 不允许写死在代码中。
- API Key 不允许提交到仓库。
- 生产环境返回设置时，应隐藏完整 API Key。
- 上传文件必须限制类型。
- 上传文件建议限制大小。
- 模型调用异常必须捕获。
- 空会议内容不允许调用模型。
- 删除操作需要确认。

## 17. 可扩展能力

系统结构需要为以下能力预留空间：

- 历史会议问答
- 跨会议检索
- 会前议程生成
- 会议背景摘要
- 会议质量检查
- 风险识别
- 任务提醒
- 纪要版本管理
- 最终确认流程
- 模板管理

这些能力可以不在第一版全部实现，但架构上不要堵死。

## 18. 开发顺序

请按以下顺序开发：

1. 搭建 FastAPI 后端。
2. 搭建前端页面。
3. 实现基础数据存储。
4. 实现部门管理。
5. 实现人员管理。
6. 实现会议创建和会议列表。
7. 实现会议详情。
8. 实现设置页面。
9. 实现三类模型保存。
10. 实现三类模型测试。
11. 实现会议文本输入。
12. 实现文件上传和解析。
13. 实现语音识别。
14. 实现 RAG 参考资料处理。
15. 实现摘要生成。
16. 实现结构化纪要抽取。
17. 实现结构化纪要质检补全。
18. 实现自动生成任务。
19. 实现日程任务页面。
20. 实现 JSON 编辑保存。
21. 实现 Word 导出。
22. 完善错误处理和加载反馈。
23. 做完整功能验收。

## 19. 验收标准

最终系统必须满足：

- 后端服务可以启动。
- 前端页面可以打开。
- 可以创建部门。
- 可以创建人员。
- 可以创建会议。
- 新建会议时可以选择部门和参会人员。
- 可以保存大模型配置。
- 可以保存 Embedding 配置。
- 可以保存语音模型配置。
- 三个模型测试按钮只测试自己的模型。
- 本地语音模式不显示 API Key 和 Base URL。
- 可以输入会议文本生成纪要。
- 可以上传 txt/docx 生成纪要。
- 可以上传 mp3/wav 生成纪要。
- 可以上传知识库参考资料辅助生成。
- 可以查看摘要。
- 可以查看结构化纪要。
- 可以查看自动生成的待办任务。
- 可以查看会议原文。
- 可以编辑并保存 JSON。
- 可以在日程任务页查看任务。
- 可以导出 Word。
- 所有耗时操作都有反馈。
- 失败时能看到明确错误原因。
- 页面刷新后核心数据不丢失。

## 20. 最终交付物

请交付：

- 后端源码
- 前端源码
- 启动说明
- API 文档
- 配置说明
- 数据存储说明
- 模型配置说明
- 功能验收说明

最终结果必须是一个完整可运行的会议智能体系统，而不是一个单页会议纪要生成 demo。

